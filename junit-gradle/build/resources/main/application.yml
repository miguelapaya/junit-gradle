server:
  port: 8001
spring:
  datasource:
    url: jdbc:mysql://localhost:3307/test_db
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect